receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  sentry:
  sentry/2:
    DSN: https://key@host/path/id

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [sentry]
