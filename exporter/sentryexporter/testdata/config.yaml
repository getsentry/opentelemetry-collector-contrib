receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  sentry:
  sentry/2:
    DSN: https://key@host:port/1

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [sentry]
