receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  sentry:
  sentry/2:
    DSN: https://publicKey:secretKey@host/path/id

service:
  pipelines:
    traces:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [sentry]
